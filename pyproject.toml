[project]
name = "fast-clip"
version = "0.1.0"
description = "Fast video assembly tool from JSON scripts"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["moviepy>=2.0.0", "pydantic>=2.0.0"]
